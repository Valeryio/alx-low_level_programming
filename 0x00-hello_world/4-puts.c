#include <stdio.h>

/**
 * main - display a text
 *
 * Description: A function used to display a text
 * Return: 0;
 */

int main(void)
{
	char chain[] = "Programming is like building a multilingual puzzle";

	puts(chain);

	return (0);
}
