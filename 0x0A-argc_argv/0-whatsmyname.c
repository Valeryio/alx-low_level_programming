#include <stdio.h>

/**
 * main - print the name of the program
 * @argc: (int), the size of the string
 * @argv: (*string), array of string
 *
 * Description: This function prints its name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
